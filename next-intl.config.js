/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  // Define the locales your app supports
  locales: ['en', 'ru'],
  // Set the default locale
  defaultLocale: 'en',
  // Where the translation messages are located (optional, defaults to "i18n")
  // messagesDir: 'messages',
  // Enable shallow routing for locale changes (optional)
  // shallowRouting: true,
};

