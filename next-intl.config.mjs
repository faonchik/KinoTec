// next-intl configuration (ESM)
/** @type {import('next-intl').NextIntlConfig} */
export default {
  // Supported locales
  locales: ['en', 'ru'],
  // Default locale
  defaultLocale: 'en',
  // messagesDir: 'messages', // optional
  // shallowRouting: true, // optional
};
